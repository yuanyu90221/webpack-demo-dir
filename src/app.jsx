import React from 'react';
import ReactDOM from 'react-dom';

// create a DOM
let element = (<h1>First React!</h1>);

// render the element into app
ReactDOM.render(
  element,
  document.getElementById('app')
);
